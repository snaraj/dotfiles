# splits first = it is the active layout, so launch --location=vsplit/hsplit
# actually splits; every other layout stays available via layout cycling.
enabled_layouts splits,*
draw_minimal_borders yes

window_padding_width 4
