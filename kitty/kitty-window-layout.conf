# All stock layouts, splits first: new windows start in splits, so the
# cmd+shift+arrow maps split in the arrow's direction. Cycling (cmd/ctrl+
# shift+l) walks kitty's normal layout ring; only the splits layout honors
# directional splitting, so cycle back to it before splitting again.
enabled_layouts splits,*
draw_minimal_borders yes

window_padding_width 4
