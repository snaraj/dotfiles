# Claude instructions for ~/.config

- You may create, edit, move, and delete files in this repo at free will — no need to ask first.
- EXCEPTION: never run `git add`, `git commit`, or `git push` unless I explicitly ask you to in that conversation. Staging, committing, and pushing are always my call.
